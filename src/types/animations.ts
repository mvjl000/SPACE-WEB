export type Initial = {
  opacity?: number;
  x?: string;
  y?: string;
};

export type Animate = {
  opacity?: number;
  x?: string;
  y?: string;
};

export type Transiton = {
  delay?: number;
  duration: number;
};
